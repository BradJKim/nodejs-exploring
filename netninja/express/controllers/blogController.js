const Blog = require("../models/blog")

const blox_index = (req, res) => {
    try {
        // blog find/rendering logic
    }
    catch(err) {
        console.log(err)
    }
}

module.exports = {
    blox_index,
}